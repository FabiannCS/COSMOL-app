import 'package:flutter/material.dart';

/// Paleta de colores oficial e institucional de COSMOL R.L. basada en la guía de diseño.
class AppColors {
  AppColors._();

  // Colores Principales Corporativos (Basados en vista_login_cosmol.txt)
  static const Color primary = Color(0xFF003E6B);
  static const Color primaryContainer = Color(0xFF005691);
  static const Color primaryBlue = Color(0xFF003E6B);
  static const Color darkNavy = Color(0xFF0B1C30);
  static const Color secondary = Color(0xFF00658C);
  static const Color accentTurquoise = Color(0xFF00A8B5);
  static const Color lightBackground = Color(0xFFF8F9FF);
  static const Color cardSurface = Color(0xFFFFFFFF);
  static const Color pureWhite = Color(0xFFFFFFFF);

  // Tipografía y Jerarquía
  static const Color onSurface = Color(0xFF0B1C30);
  static const Color onSurfaceVariant = Color(0xFF414750);
  static const Color textMuted = Color(0xFF727781);
  static const Color outlineVariant = Color(0xFFC1C7D1);

  // Aliases de compatibilidad para estilos globales
  static const Color textPrimary = Color(0xFF0B1C30);
  static const Color textSecondary = Color(0xFF414750);

  // Superficies y Alertas
  static const Color surfaceContainerLow = Color(0xFFEFF4FF);
  static const Color surfaceContainerHigh = Color(0xFFDCE9FF);
  static const Color borderSubtle = Color(0xFFE2E8F0);
  static const Color borderStrong = Color(0xFFCBD5E1);
  static const Color border = Color(0xFFE2E8F0);

  // Semáforos y Estados
  static const Color successGreen = Color(0xFF16A34A);
  static const Color successBackground = Color(0xFFF0FDF4);

  static const Color error = Color(0xFFDC2626);
  static const Color errorRed = Color(0xFFDC2626);
  static const Color errorContainer = Color(0xFFFEF2F2);
  static const Color errorBackground = Color(0xFFFEF2F2);

  static const Color warningOrange = Color(0xFFF57C00);
  static const Color warningBackground = Color(0xFFFFF3E0);

  // Sombras y Capas
  static const Color shadowColor = Color(0x0F003E6B);
  static const Color barrierColor = Color(0x730B1C30);
}
